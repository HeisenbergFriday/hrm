# 钉钉登录修复 - 快速操作清单

## ✅ 已完成

1. ✅ 代码修复：增强日志和改进匹配逻辑
2. ✅ 代码编译：无语法错误
3. ✅ 创建 PR：https://github.com/HeisenbergFriday/hrm/pull/10
4. ✅ 文档编写：诊断指南、部署步骤、修复总结

## 🚀 接下来需要你做的

### 第一步：合并 Pull Request

访问 https://github.com/HeisenbergFriday/hrm/pull/10

点击 **"Merge pull request"** 按钮

### 第二步：连接测试服务器

```bash
ssh -p 16388 ubuntu@113.240.65.185
```

### 第三步：更新并重启服务

```bash
# 进入项目目录
cd /home/ubuntu/peopleops-hr-test

# 拉取最新代码
git pull origin master

# 停止服务
docker compose -p peopleops-hr-test -f docker-compose.test.yml down

# 重新构建（包含新代码）
docker compose -p peopleops-hr-test -f docker-compose.test.yml build --no-cache

# 启动服务
docker compose -p peopleops-hr-test -f docker-compose.test.yml up -d

# 验证服务启动
docker compose -p peopleops-hr-test -f docker-compose.test.yml ps
```

### 第四步：测试登录并查看日志

**开启日志监控**（在另一个 SSH 终端）：
```bash
cd /home/ubuntu/peopleops-hr-test
docker compose -p peopleops-hr-test -f docker-compose.test.yml logs -f | grep "dingtalk/callback"
```

**测试登录**：
1. 打开浏览器，清除缓存和 Cookie
2. 访问登录页面
3. 选择钉钉扫码登录
4. 观察日志输出

### 第五步：分析日志

日志会告诉你：

#### ✅ 成功的情况
```
[dingtalk/callback] user_info_raw: {...}
[dingtalk/callback] parsed: associated_user_id=xxx, unionid=yyy, openid=zzz
[dingtalk/callback] user found by userid: user_id=xxx
```
→ **登录成功！**

#### ❌ 失败的情况

**情况 1：用户未同步**
```
[dingtalk/callback] user not found: identity=xxx, name=张三, email=xxx@example.com
```
→ **解决方案**：在后台执行"同步组织数据"

**情况 2：API 调用失败**
```
[dingtalk/callback] GetUserInfoByCode failed: xxx
```
→ **解决方案**：检查钉钉应用配置（AppKey、AppSecret、权限）

**情况 3：缺少关键信息**
```
[dingtalk/callback] user_info_raw: {"nick":"xxx"}  // 缺少 email、mobile
[dingtalk/callback] parsed: associated_user_id=, unionid=, openid=
```
→ **解决方案**：检查钉钉应用权限，需要 `Contact.User.Read`

## 📋 检查清单

执行部署后，请确认：

- [ ] PR 已合并到 master
- [ ] 代码已拉取到服务器
- [ ] 服务已重新构建并启动
- [ ] 容器状态正常（`docker ps` 显示都在运行）
- [ ] 可以访问登录页面
- [ ] 日志实时监控已开启
- [ ] 至少进行了一次登录测试
- [ ] 日志中看到了 `[dingtalk/callback]` 相关输出

## 🆘 如果遇到问题

### 容器启动失败
```bash
# 查看详细错误
docker compose -p peopleops-hr-test -f docker-compose.test.yml logs
```

### 日志没有输出
```bash
# 确认服务是否运行
docker compose -p peopleops-hr-test -f docker-compose.test.yml ps

# 查看所有日志
docker compose -p peopleops-hr-test -f docker-compose.test.yml logs -f
```

### 需要回滚
```bash
cd /home/ubuntu/peopleops-hr-test

# 查看提交历史
git log --oneline -5

# 回滚到之前的版本（找到之前的 commit hash）
git reset --hard <previous-commit-hash>

# 重新构建
docker compose -p peopleops-hr-test -f docker-compose.test.yml down
docker compose -p peopleops-hr-test -f docker-compose.test.yml build --no-cache
docker compose -p peopleops-hr-test -f docker-compose.test.yml up -d
```

## 📄 参考文档

部署过程中可以参考这些文档（已在项目中）：

1. **诊断指南**: `docs/钉钉登录问题诊断指南.md`
   - 详细的问题排查步骤
   - 日志分析方法
   - 钉钉应用配置检查

2. **部署步骤**: `deploy/测试服务器部署步骤.md`
   - 完整的部署流程
   - 常见问题处理
   - 回滚方案

3. **修复总结**: `docs/钉钉登录问题修复总结.md`
   - 修复内容说明
   - 技术细节
   - 预期效果

## 💬 反馈

部署和测试完成后，请告诉我：

1. ✅ 登录成功了吗？
2. 📊 日志显示了什么？
3. ❓ 遇到了什么问题？

我会根据实际情况提供进一步的支持！

---

**快速访问**:
- PR: https://github.com/HeisenbergFriday/hrm/pull/10
- 测试服务器: `ssh -p 16388 ubuntu@113.240.65.185`
